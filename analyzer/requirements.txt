# Analyzer runs on demand on a Mac (MPS) or a cloud GPU. Nothing here is a service.
yt-dlp>=2024.8
ultralytics>=8.3
opencv-python-headless>=4.10
numpy>=1.26
scikit-learn>=1.5
supabase>=2.6
python-dotenv>=1.0
tqdm>=4.66
# YOLO-World text prompts (built once by get_weights.sh)
git+https://github.com/ultralytics/CLIP.git
