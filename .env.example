# Optional. Defaults live in src/config.ts (Supabase URL + publishable key are public by design).
# Set these only to point a build at a different Supabase project or admin email.
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
VITE_ADMIN_EMAIL=
